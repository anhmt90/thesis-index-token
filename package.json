{
  "name": "thesis-index-token",
  "version": "1.0.0",
  "description": "",
  "main": "truffle-config.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "compile": "truffle compile",
    "migrate": "truffle migrate",
    "chain": "ganache-cli --allowUnlimitedContractSize -l 90000000 -d",
    "deploy": "env-cmd -f config/.env node -r dotenv/config src/deploy.js main",
    "test": "env-cmd -f config/.env.test mocha --timeout 10000"
  },
  "author": "Tuan Anh Ma",
  "license": "ISC",
  "dependencies": {
    "@chainlink/token": "^1.1.0",
    "@openzeppelin/contracts": "^4.1.0",
    "@uniswap/v2-core": "^1.0.1",
    "@uniswap/v2-periphery": "^1.1.0-beta.0",
    "coingecko-api-v3": "0.0.11",
    "env-cmd": "^10.1.0",
    "ganache-cli": "^6.12.2",
    "mocha": "^8.4.0",
    "solc": "^0.8.4",
    "web3": "^1.3.5"
  },
  "devDependencies": {
    "wtfnode": "^0.8.4"
  }
}
